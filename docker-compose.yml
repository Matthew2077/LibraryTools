services:
  web:
    build: .
    ports:
      -"5000:5000"
    environment:
      - name=value
    depends_on:
      - db
  db:
    imagine: postgres:15
    environment:
    - name=value
    volumes:
      -pgdata
volumes:
 -pgdata

# WORKING IN PROGRESS
